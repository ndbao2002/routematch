# Dispatch and State Machine Engine

Consumes MatchedAssignments, locks drivers, issues offers, and resolves accept/reject timeouts.
Publishes OrdersFinalized and OrdersFailed for the gateway sync worker.

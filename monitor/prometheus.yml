global:
  scrape_interval: 5s # Scrape frequently for this demo

scrape_configs:
  - job_name: 'routematch_dispatcher'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['host.docker.internal:9000'] # Or 'localhost:9000' if running locally